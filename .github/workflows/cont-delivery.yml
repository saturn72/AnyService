name: anyservice ci

on:
  push:
    branches:
      - master

jobs:
  continunous_delivery:
    name: build, test & deploy
    runs-on: ubuntu-latest
    steps:
      - name: Setup .NET Core
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 3.0.100

      - name: Checkout master branch
        uses: actions/checkout@master
        with:
          dotnet-version: 3.1.100

      - name: Build
        working-directory: ./src
        run: dotnet build --configuration Release

      - name: Test
        working-directory: ./src
        run: dotnet test --configuration Release --no-build
      # - name: Pack anyservice nuget package
      #   working-directory: ./src
      #   run: dotnet pack ./AnyService/AnyService.csproj --configuration Release --output ./nupkg --no-build --include-symbols -p:SymbolPackageFormat=snupkg
      # - name: Pack anyservice.core nuget package
      #   working-directory: ./src
      #   run: dotnet pack ./AnyService.Core/AnyService.Core.csproj --configuration Release --output ./nupkg --no-build --include-symbols -p:SymbolPackageFormat=snupkg
      # - name: Pack anyservice.litedb nuget package
      #   working-directory: ./src
      #   run: dotnet pack ./AnyServiceModules/LiteDb/AnyService.LiteDb/AnyService.LiteDb.csproj --configuration Release --output ./nupkg --no-build --include-symbols -p:SymbolPackageFormat=snupkg
      # - name: Pack anyservice.entityframework nuget package
      #   working-directory: ./src
      #   run: dotnet pack ./AnyServiceModules/EntityFramework/AnyService.EntityFramework/AnyService.EntityFramework.csproj --configuration Release --output ./nupkg --no-build --include-symbols -p:SymbolPackageFormat=snupkg
      # - name: Pack anyservice.easycaching nuget package
      #   working-directory: ./src
      #   run: dotnet pack ./AnyServiceModules/EasyCaching/AnyService.EasyCaching/AnyService.EasyCaching.csproj --configuration Release --output ./nupkg --no-build --include-symbols -p:SymbolPackageFormat=snupkg
      # - name: Push packages to nuget
      #   working-directory: ./src/nupkg
      #   run: dotnet nuget push *.nupkg --api-key $nuget_api_key --source https://api.nuget.org/v3/index.json
      #   env:
      #     nuget_api_key: ${{secrets.NUGET_DEPLOY_KEY}}
